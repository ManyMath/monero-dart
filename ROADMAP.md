# Roadmap

# TODOs

- Extract /rust (the monero_ffi crate) into a separate repository, but integrated into monero-dart as a subtree (or similar) so that recursive submodule initialization isn't required.
- Use monero-ffi as the crate name but monero_ffi as the library name.
