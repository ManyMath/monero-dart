# FFI bindings configuration for monero_ffi
name: 'MoneroFfi'
description: 'Bindings to monero_ffi native library'
output: 'lib/monero_ffi.g.dart'
headers:
  entry-points:
    - 'rust/target/monero_ffi.h'
functions:
  include:
    - 'sum'
    - 'sum_long_running'
